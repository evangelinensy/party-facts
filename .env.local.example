JWT_SECRET=replace-this-with-a-random-32-character-string

# Vercel KV — only needed when deploying to Vercel.
# Locally the app uses an in-memory store (no setup required).
# In Vercel dashboard: Storage → Create KV store → these env vars are auto-added.
KV_REST_API_URL=
KV_REST_API_TOKEN=
